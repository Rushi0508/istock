<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Payments</title>
    <link rel="stylesheet" href="/pendingsty.css">
</head>
<body>
    <h1>Showing paid DATA</h1>
    <button><a href="/khatabook">Back</a></button>
    <ol>
        <% for(user of users){ %>
            <% if(user.paymentStatus === "paid") {%>
        <li>
            Name : <%= user.name %><br>
            Amount : <%= user.amount %><br>
            Payment status : <%= user.paymentStatus %><br>
            Borrow date : <%= user.borrowdate %>
        </li>
        <%} } %>
    </ol>
</body>
</html>
